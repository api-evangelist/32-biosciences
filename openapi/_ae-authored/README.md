# Written by API Evangelist, not harvested from the provider

The documents in this directory were NOT published by 32 Biosciences. They were derived
mechanically by the API Evangelist enrichment pipeline from the WordPress route-discovery
document the company's site serves at `https://32biosciences.com/wp-json/` (saved verbatim in
`openapi/_source/32-biosciences-wp-json-root.json`).

Each carries an explicit authorship marker in `info`:

```yaml
x-derived-by: API Evangelist enrichment pipeline
x-derived-from: https://32biosciences.com/wp-json/
x-derived-on: '2026-09-05'
x-provider-published: false
```

The Kin Score credits the presence of `openapi/_original/` as evidence the PROVIDER published a
contract, and grades a marked document as `derived` separately. Nothing here belongs in
`_original/`: 32 Biosciences publishes no OpenAPI definition of any kind.

The per-tag files at `openapi/` are tag-splits of `32-biosciences-content-openapi.yml` in this
directory and carry the same markers.
